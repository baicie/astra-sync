package io.astrasync.connector.api;

/** Public scalar types supported by descriptor-driven connector configuration. */
public enum ConnectorOptionType {
    STRING,
    INTEGER,
    BOOLEAN,
    DURATION,
    ENUM
}
